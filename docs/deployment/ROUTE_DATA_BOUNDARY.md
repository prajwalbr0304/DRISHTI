# DRISHTI Route-to-Data-Boundary Inventory

> Generated by `services/ml/tools/route_data_boundary.py` from the live FastAPI route table and `app/deployment_boundary.py`. Do not edit by hand.

- Generated: 2026-07-21 09:29 UTC
- Total deployed routes: **335**
- Domains: **33**
- Boundary check: **PASS**

## Route counts by data boundary

| Data boundary | Routes |
| --- | ---: |
| CATALYST_OPERATIONAL | 254 |
| AWS_ANALYTICS | 69 |
| DISABLED_OUT_OF_SCOPE | 4 |
| LOCAL_TOOLING_ONLY | 0 |
| INFRASTRUCTURE | 8 |

## CATALYST_OPERATIONAL — Catalyst Data Store / Stratus

### `admin` (23 routes)

- Backend: Catalyst Data Store/code reads (done); RDS for the rest · migration: `rds_backed_migration_pending`
- Rationale: Admin/governance console. Feature flags + usage/plan baseline are code-based config (work without DATABASE_URL); the deeper governance read models still use RDS (tracked closure).
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: admin.service

### `board` (34 routes)

- Backend: Catalyst Data Store canvas (native) + Stratus export; RDS/graph hydration pending adapter · migration: `rds_backed_migration_pending`
- Rationale: Investigation Board CANVAS (board/node/edge/annotation/activity) persists natively in Data Store (board/repo.py) and runs offline. Emergency-Response references already hydrate from Data Store. Search Around now fetches through the protected AWS analytics adapter (app/analytics_adapter.py) when configured (deployed), falling back to a direct RDS read only in dev. Crime-corpus reference hydration still reads RDS (curated Data Store projection is the target). Tracked, not hidden (C.3).
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: board.promote, board.references, board.searcharound, board.service

### `cases` (10 routes)

- Backend: Catalyst Data Store (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: Case detail/read models. Case corpus embeddings/similarity are AWS analytics; the operational case record is a Data Store target.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: cases.corpus, cases.explorer, cases.service

### `casework` (20 routes)

- Backend: Catalyst Data Store reads (lookups done); writes RDS today · migration: `rds_backed_migration_pending`
- Rationale: Statements/property/court/lifecycle. Lookups are code/Data Store-served (work without DATABASE_URL); the record WRITE paths still use RDS (tracked closure).
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: casework.service

### `chat` (6 routes)

- Backend: Catalyst Data Store (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: Assistant chat session/message metadata (data-minimised).
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: chat.service

### `disaster` (31 routes)

- Backend: Catalyst Data Store (native) · migration: `datastore_native`
- Rationale: Emergency Response hazards/readings/forecasts/resources/routes persist natively in Data Store (disaster/repo.py over the shared DataStoreRepository). The AWS PostGIS mirror is reconstructable and adapter-only.

### `evidence` (16 routes)

- Backend: Catalyst Data Store metadata + Stratus objects (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: Evidence metadata (manual only; no extraction). File bytes -> private Stratus; metadata -> Data Store. Currently RDS metadata.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: evidence.guards, evidence.service

### `governance` (13 routes)

- Backend: Catalyst Data Store (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: Governance artifact/audit read models.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: governance.builder, governance.service

### `identity` (23 routes)

- Backend: Catalyst Data Store (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: Canonical person/entity operational records.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: identity.service

### `imports` (17 routes)

- Backend: Catalyst Data Store reads (done) + Stratus; commit RDS today · migration: `rds_backed_migration_pending`
- Rationale: Import templates + financial account/transaction views now read from Catalyst Data Store (seeded serving subset; work without DATABASE_URL). The import COMMIT/canonicalization write pipeline still uses RDS + Stratus objects (tracked closure).
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: imports.analytics, imports.service

### `intake` (19 routes)

- Backend: Catalyst Data Store reads (done) + Stratus; writes RDS today · migration: `rds_backed_migration_pending`
- Rationale: FIR/case intake. Reference lookups + workflow metadata now read from Catalyst Data Store (seeded serving subset) and work with DATABASE_URL absent. The canonical FIR WRITE path (create/submit/approve) still uses the RDS serving schema and is the tracked remaining closure.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: intake.guards, intake.lookups, intake.service

### `internal` (5 routes) · not in submitted UI

- Backend: Catalyst Data Store (service-scope) · migration: `datastore_native`
- Rationale: Service-to-service endpoints (event/cron targets). Require a signed service context; persist authoritative Data Store state for prediction dispatch / notify / forecast run.

### `investigate` (3 routes)

- Backend: Catalyst Data Store (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: Prompt 20 case-scoped investigation assistant: thin orchestration over case summary/similar/identity/leads/timeline that sends cited objects to the Board. Reads the operational case corpus (RDS today; Data Store target).
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: investigate.service

### `livefeed` (3 routes)

- Backend: Catalyst Data Store projection (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: Live Command Center committed-FIR projection/freshness. Idempotent projection; no person rescore, no auto-dispatch.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: livefeed.flow

### `notifications` (9 routes)

- Backend: Catalyst Data Store (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: In-app notifications/work items.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: notifications.service

### `org` (10 routes)

- Backend: Catalyst Data Store (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: Organizational hierarchy + provisioning. Scope derivation is a pure server-side function; the establishment lookup is a Data Store target.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: org.service

### `reports` (6 routes)

- Backend: Catalyst Data Store + Stratus (target); RDS today · migration: `rds_backed_migration_pending`
- Rationale: Report snapshots. Rendered report objects -> private Stratus.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: reports.service

### `scenarios` (5 routes)

- Backend: Catalyst Data Store (native) / bundled fixtures · migration: `datastore_native`
- Rationale: Prompt 20 synthetic scenario registry is additive fixture data loaded at runtime; no RDS access.

### `search` (1 routes)

- Backend: Catalyst Data Store full-text search · migration: `datastore_native`
- Rationale: Deployed operational metadata search over Data Store search columns (case/FIR/person/evidence).

## AWS_ANALYTICS — protected server-to-server adapter only

### `analytics` (2 routes)

- Backend: AWS RDS via protected adapter
- Rationale: Cross-district correlation/socioeconomic patterns (aggregate, k-anonymised, correlational-not-causal).
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: analytics.service

### `explain` (4 routes)

- Backend: AWS RDS via protected adapter
- Rationale: Analytics explainability over derived model artifacts.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: explain.service

### `forecast` (9 routes)

- Backend: AWS RDS via protected adapter
- Rationale: Spatiotemporal forecasting/backtests. Derived historical + model compute; adapter-only.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: forecast.earlywarning, forecast.fusion, forecast.governance_bridge, forecast.nearrepeat, forecast.service, forecast.stgnn, forecast.tabfm_forecast, forecast.timesfm

### `geo` (14 routes)

- Backend: AWS RDS/PostGIS via protected adapter
- Rationale: PostGIS geospatial hotspots/trends/jurisdiction. Boundaries are curated projections; spatial queries are adapter-only.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: geo.alerts, geo.hotspots, geo.jurisdiction, geo.persist, geo.router, geo.service

### `graph` (16 routes)

- Backend: AWS RDS via protected adapter
- Rationale: Graph algorithms/community/hidden-link analytics over the full graph corpus. Reviewed projections may be surfaced via Data Store; heavy queries stay adapter-only.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: graph.algorithms, graph.archive, graph.enrich, graph.explorer, graph.hidden, graph.service

### `money` (4 routes)

- Backend: AWS RDS via protected adapter
- Rationale: Financial-trail detection/graph analytics.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: money.detection, money.service

### `performance` (1 routes)

- Backend: AWS RDS via protected adapter
- Rationale: Prompt 20 supervisor station/officer performance metrics (derived aggregate; scoped server-side).
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: performance.service

### `predict` (8 routes)

- Backend: AWS RDS + custom ML via protected adapter
- Rationale: Prediction runtime metadata (open aggregate) + job submit/collect routed to the protected adapter / custom-ML plane.

### `risk` (3 routes)

- Backend: AWS RDS via protected adapter
- Rationale: Area/period risk scoring (decision support; never individual operationalised risk).
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: risk.scoring, risk.service

### `workload` (8 routes)

- Backend: AWS RDS via protected adapter
- Rationale: Workload-band benchmark/evaluation over historical caseload.
- Direct PostgreSQL-connector imports [expected (RDS-backed, migration pending)]: workload.governance_bridge, workload.service

## DISABLED_OUT_OF_SCOPE — hidden from the submitted UI

### `rag` (3 routes) · not in submitted UI

- Backend: Catalyst Data Store KB when configured; else honest empty state
- Rationale: Optional retrieval-augmented knowledge base. Disabled/empty state unless a knowledge base is configured; hidden from the mandatory demo journey.
- Direct PostgreSQL-connector imports [VIOLATION (native domain must not import the connector)]: rag.service

### `stream` (1 routes) · not in submitted UI

- Backend: n/a (SSE channel; off by default)
- Rationale: Scoped SSE channel; OFF unless DRISHTI_STREAM_CHANNEL_ENABLED. Its own short-lived channel-token auth, not a data path.

## INFRASTRUCTURE — health/docs (no application data)

### `fastapi.applications` (4 routes) · not in submitted UI

- Backend: OpenAPI/docs (framework)
- Rationale: Framework-generated /openapi.json, /docs, /redoc. No app data.

### `main` (4 routes) · not in submitted UI

- Backend: process/health only
- Rationale: Root, /health, /health/live, /health/ready, and the /demo/risk-score plumbing proof. Health probes are hit directly by Catalyst infra; /demo/* is not in the submitted UI.
- Direct PostgreSQL-connector imports [VIOLATION (native domain must not import the connector)]: main
