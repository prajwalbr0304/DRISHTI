psycopg2-binary>=2.9.9
numpy>=1.26
python-dotenv>=1.0.1

# Geospatial boundary handling for the data generator (datagen/boundaries.py):
# real Karnataka state/district/taluk polygons drive point-in-polygon placement
# and Voronoi-derived SHO (police-station jurisdiction) regions.
shapely>=2.0
scipy>=1.11
