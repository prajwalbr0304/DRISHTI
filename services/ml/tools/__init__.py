"""DRISHTI ML service developer tooling (route boundary, authz matrix).

Importable so the test suite can exercise the same case data + decision logic
the artifact generators use (keeps the generated artifacts and CI in lock-step).
"""
