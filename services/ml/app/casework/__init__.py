"""Casework: structured digital statements, property/seizure, lab-result
metadata, court events/bail/disposition, event-backed lifecycle + timeline
(Phase 7).

Everything is entered MANUALLY through typed forms/APIs. No OCR / speech-to-text
/ automated parsing. An uploaded file may be LINKED as a reference (EvidenceItem)
but its content never auto-populates these records. Case status is derived from
append-only events; verified outcomes come only from final events.
"""
