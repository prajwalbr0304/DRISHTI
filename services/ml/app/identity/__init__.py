"""Canonical identity + entity-resolution module (Phase 4).

Stable CanonicalPerson/CanonicalOrganisation/CanonicalEntity identity, case-party
roles bound to canonical ids, governed aliases/identifiers/contacts/addresses,
human-reviewed entity resolution (NEVER auto-merge), and reversible merge/split.
"""
