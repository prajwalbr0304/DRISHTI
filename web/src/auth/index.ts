export { AuthProvider, RequireAuth, useAuth, useAuthOptional } from "@/auth/AuthProvider";
export { deriveDisplayRole } from "@/auth/roleMapping";
export type { AuthMode, AuthStatus, AuthUser } from "@/auth/types";
